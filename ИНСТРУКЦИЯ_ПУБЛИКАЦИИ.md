# ИНСТРУКЦИЯ ПО ПУБЛИКАЦИИ ПРОЕКТА НА GITHUB

## 📋 Подготовка проекта перед публикацией

### 1. Проверка файлов

Убедитесь, что следующие файлы присутствуют и актуальны:

✅ **Обязательные файлы:**
- `README.md` - основная документация
- `Dockerfile` - образ для сборки
- `docker-compose.yml` - конфигурация для запуска
- `package.json` - зависимости Node.js
- `tsconfig.json` - конфигурация TypeScript
- `prisma/schema.prisma` - схема базы данных
- `prisma/migrations/` - миграции БД
- `.gitignore` - исключения для Git
- `.dockerignore` - исключения для Docker

✅ **Документация:**
- `ТЕХНИЧЕСКИЕ_ТРЕБОВАНИЯ.md`
- `АРХИТЕКТУРА_БОТА.md`
- `ПОЛЬЗОВАТЕЛЬСКИЕ_СЦЕНАРИИ.md`

✅ **Исходный код:**
- `src/` - весь исходный код
- `prisma/` - схема и миграции

✅ **Тесты:**
- `src/**/__tests__/` - автоматические тесты (Jest)
- `jest.config.js` - конфигурация тестов

### 2. Проверка .gitignore

Убедитесь, что `.gitignore` исключает:
- `node_modules/`
- `dist/` (скомпилированный код)
- `.env` (секреты)
- `certs/*.crt`, `certs/*.pem` (сертификаты)
- `coverage/` (отчеты о покрытии тестами)
- Логи и временные файлы

### 3. Обновление README.md

Проверьте, что в README.md указаны правильные пути:
- Замените `<ваш-username>` на ваш GitHub username
- Замените `<название-репозитория>` на название репозитория

---

## 🚀 Пошаговая инструкция публикации

### Шаг 1: Создание репозитория на GitHub

1. Перейдите на [GitHub](https://github.com) и войдите в аккаунт
2. Нажмите кнопку **"New repository"** (или перейдите по ссылке: https://github.com/new)
3. Заполните форму:
   - **Repository name**: `max-productivity-bot` (или другое название)
   - **Description**: `AI-ассистент для мессенджера MAX: дайджесты, дедлайны, напоминания`
   - **Visibility**: 
     - `Public` - если проект открытый
     - `Private` - если проект приватный (для конкурса может быть нужен публичный)
   - **НЕ** инициализируйте репозиторий с README, .gitignore или лицензией (у вас уже есть эти файлы)
4. Нажмите **"Create repository"**

### Шаг 2: Инициализация Git в локальном проекте

Откройте терминал в корне проекта и выполните:

```bash
# Проверьте, не инициализирован ли уже Git
git status

# Если Git не инициализирован, выполните:
git init

# Настройте имя пользователя и email (если еще не настроено глобально)
git config user.name "Ваше Имя"
git config user.email "ваш.email@example.com"
```

### Шаг 3: Добавление файлов в Git

```bash
# Добавьте все файлы (кроме тех, что в .gitignore)
git add .

# Проверьте, что будет закоммичено (убедитесь, что нет секретов)
git status

# Если видите файлы, которые не должны быть в репозитории (например, .env):
# Удалите их из индекса:
# git reset HEAD .env
# И убедитесь, что они в .gitignore
```

### Шаг 4: Создание первого коммита

```bash
git commit -m "Initial commit: MAX Productivity Bot

- Полная реализация AI-ассистента для MAX
- Docker-образ с multi-stage build
- Интеграция с GigaChat API
- Поддержка дайджестов, задач, дедлайнов
- Персональный ИИ-ассистент
- Экспорт календаря (ICS и Excel)
- Полная документация"
```

### Шаг 5: Подключение к удаленному репозиторию

```bash
# Добавьте удаленный репозиторий (замените YOUR_USERNAME и REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/REPO_NAME.git

# Проверьте подключение
git remote -v
```

### Шаг 6: Переименование ветки (если нужно)

```bash
# Переименуйте текущую ветку в main (если она называется master)
git branch -M main
```

### Шаг 7: Публикация на GitHub

```bash
# Отправьте код на GitHub
git push -u origin main

# Если возникнет ошибка аутентификации:
# 1. Используйте Personal Access Token вместо пароля
# 2. Или настройте SSH ключи
```

---

## 🔐 Настройка аутентификации GitHub

### Вариант 1: Personal Access Token (рекомендуется)

1. Перейдите в GitHub Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Нажмите **"Generate new token (classic)"**
3. Выберите права доступа:
   - `repo` (полный доступ к репозиториям)
   - `write:packages` (для публикации Docker-образов)
4. Скопируйте токен
5. При `git push` используйте токен вместо пароля

### Вариант 2: SSH ключи

```bash
# Сгенерируйте SSH ключ (если еще нет)
ssh-keygen -t ed25519 -C "ваш.email@example.com"

# Добавьте ключ в ssh-agent
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

# Скопируйте публичный ключ
cat ~/.ssh/id_ed25519.pub

# Добавьте ключ в GitHub: Settings → SSH and GPG keys → New SSH key
```

---

## 🐳 Настройка автоматической сборки Docker-образа

После публикации кода GitHub Actions автоматически соберет Docker-образ.

### Проверка работы GitHub Actions

1. Перейдите в репозиторий на GitHub
2. Откройте вкладку **"Actions"**
3. Должен появиться workflow **"Build and Push Docker Image"**
4. После успешной сборки образ будет доступен в **Packages**

### Использование опубликованного образа

После успешной сборки обновите `docker-compose.yml`:

```yaml
bot:
  image: ghcr.io/YOUR_USERNAME/REPO_NAME:latest
  # ... остальная конфигурация
```

Или используйте в README:

```bash
docker pull ghcr.io/YOUR_USERNAME/REPO_NAME:latest
```

---

## ✅ Проверка после публикации

### 1. Проверьте структуру репозитория

Убедитесь, что на GitHub есть:
- ✅ Все файлы из `src/`
- ✅ `Dockerfile` и `docker-compose.yml`
- ✅ `README.md` и документация
- ✅ `prisma/schema.prisma` и миграции
- ✅ `jest.config.js` и тесты в `src/**/__tests__/`
- ❌ НЕТ `node_modules/`
- ❌ НЕТ `dist/`
- ❌ НЕТ `.env`
- ❌ НЕТ сертификатов в `certs/`
- ❌ НЕТ `coverage/`

### 2. Проверьте README.md

- Откройте README на GitHub
- Убедитесь, что все ссылки работают
- Проверьте, что примеры команд корректны
- Замените плейсхолдеры (`<ваш-username>`, `<название-репозитория>`)

### 3. Проверьте GitHub Actions

- Перейдите в **Actions**
- Убедитесь, что workflow запустился
- Проверьте, что сборка прошла успешно

### 4. Проверьте Packages

- Перейдите в **Packages** (или нажмите на имя репозитория справа)
- Убедитесь, что Docker-образ опубликован
- Проверьте теги образа

---

## 📝 Обновление проекта

После внесения изменений:

```bash
# Проверьте изменения
git status

# Добавьте измененные файлы
git add .

# Создайте коммит
git commit -m "Описание изменений"

# Отправьте на GitHub
git push
```

GitHub Actions автоматически пересоберет Docker-образ при push в `main`/`master`.

---

## 🔧 Решение проблем

### Проблема: "Permission denied"

**Решение:**
- Проверьте аутентификацию (токен или SSH ключ)
- Убедитесь, что у вас есть права на запись в репозиторий

### Проблема: "Large files"

**Решение:**
- Проверьте `.gitignore`
- Убедитесь, что большие файлы (PDF, изображения) исключены
- Используйте Git LFS для больших файлов (если необходимо)

### Проблема: GitHub Actions не запускается

**Решение:**
- Проверьте, что файл `.github/workflows/docker-build.yml` существует
- Убедитесь, что workflow файл имеет правильный синтаксис YAML
- Проверьте права доступа в настройках репозитория

### Проблема: Docker-образ не публикуется

**Решение:**
- Проверьте логи в GitHub Actions
- Убедитесь, что `GITHUB_TOKEN` имеет права на запись в Packages
- Проверьте, что Dockerfile корректен

---

## 📚 Дополнительные ресурсы

- [GitHub Docs: Creating a repository](https://docs.github.com/en/get-started/quickstart/create-a-repo)
- [GitHub Docs: Pushing commits](https://docs.github.com/en/get-started/using-git/pushing-commits-to-a-remote-repository)
- [GitHub Container Registry](https://docs.github.com/en/packages/working-with-a-github-packages-registry/working-with-the-container-registry)

---

## ✨ Готово!

После выполнения всех шагов ваш проект будет:
- ✅ Опубликован на GitHub
- ✅ Готов к клонированию и запуску
- ✅ Автоматически собирать Docker-образ при изменениях
- ✅ Соответствовать требованиям онлайн этапа

Удачи с проектом! 🚀

