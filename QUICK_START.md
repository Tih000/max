# 🚀 БЫСТРЫЙ СТАРТ - Публикация на GitHub

## Команды для публикации

```bash
# 1. Инициализация (если еще не сделано)
git init

# 2. Добавление всех файлов
git add .

# 3. Проверка (убедитесь, что нет секретов!)
git status

# 4. Первый коммит
git commit -m "Initial commit: MAX Productivity Bot"

# 5. Подключение к GitHub (замените YOUR_USERNAME и REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# 6. Переименование ветки в main
git branch -M main

# 7. Публикация
git push -u origin main
```

## ⚠️ ВАЖНО перед публикацией

1. **Проверьте .gitignore** - убедитесь, что исключены:
   - `node_modules/`
   - `dist/`
   - `.env`
   - Сертификаты
   - `coverage/` (отчеты о покрытии тестами)

2. **Обновите README.md**:
   - Замените `<ваш-username>` на ваш GitHub username
   - Замените `<название-репозитория>` на название репозитория

3. **Проверьте, что нет секретов**:
   ```bash
   git status
   # Убедитесь, что .env НЕ в списке
   ```

## 📦 После публикации

1. GitHub Actions автоматически соберет Docker-образ
2. Образ будет доступен в **Packages** репозитория
3. Используйте: `ghcr.io/YOUR_USERNAME/REPO_NAME:latest`

## 🧪 Тестирование

Проект включает **49 автоматических тестов**:
```bash
npm test              # Запустить все тесты
npm run test:watch    # Режим watch
npm run test:coverage # С отчетом о покрытии
```

Все тесты проходят успешно ✅

## 📖 Подробная инструкция

См. файл `ИНСТРУКЦИЯ_ПУБЛИКАЦИИ.md`

